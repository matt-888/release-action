name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - uses: matt-888/release-action@main
        with:
          token: ${{ secrets.RELEASE_TOKEN }}
